/* Weekly Draw season switch.
 * To open a new draw, change open to true, set the season, and add the entry URL.
 * The previous-season winners and rules remain on the page as an archive.
 */
window.SUTTON_WEEKLY_DRAW = Object.freeze({
  open: false,
  season: 2027,
  entryUrl: "join.html"
});
