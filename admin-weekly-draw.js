(() => {
  const dashboard=document.querySelector('.admin-dashboard-section'); if(!dashboard)return;
  const section=document.createElement('section'); section.id='weekly-draw-admin'; section.className='section admin-manage-section';
  section.innerHTML=`<div class="container"><div class="section-heading"><div><p class="eyebrow dark">Weekly Draw manager</p><h2>Season settings</h2></div><a class="text-link" href="weekly-draw.html">View Weekly Draw →</a></div><div class="admin-layout"><form id="draw-admin-form" class="admin-panel"><div class="admin-panel-heading"><p class="eyebrow dark">Draw status</p><h2>Open or close entries</h2></div><label class="admin-switch-row"><span>Entries open</span><input id="draw-admin-open" type="checkbox"></label><div class="admin-grid"><label><span>Season</span><input id="draw-admin-season" type="number" min="2026" max="2100" required></label><label><span>Entry / payment link</span><input id="draw-admin-url" type="text" required placeholder="https://... or join.html"></label></div><div class="admin-actions"><button class="btn btn-primary" type="submit">Prepare settings</button></div></form><aside class="admin-panel admin-help"><p class="eyebrow dark">Publishing</p><h2>One simple setting</h2><p>Use this form when the next Weekly Draw is ready. Opening entries will make the yellow <strong>Entries are now open</strong> banner appear on the public Weekly Draw page.</p><ol><li>Choose Open or Closed.</li><li>Set the season and entry/payment link.</li><li>Click Prepare settings.</li><li>Copy the config and replace <strong>weekly-draw-config.js</strong> on GitHub.</li></ol><p class="admin-note">Keep entries closed until the real 2027 entry or payment link is ready.</p></aside></div><div id="draw-admin-publish" class="admin-publish-actions" hidden><button id="draw-admin-copy" class="btn btn-primary" type="button">1. Copy config</button><a class="btn btn-outline dark-outline" href="https://github.com/fauxy87/sutton-cricket-club/blob/main/weekly-draw-config.js" target="_blank" rel="noopener">2. Open config on GitHub</a><span id="draw-admin-status" class="admin-status"></span></div></div>`;
  dashboard.insertAdjacentElement('afterend',section);
  const form=document.getElementById('draw-admin-form'),open=document.getElementById('draw-admin-open'),season=document.getElementById('draw-admin-season'),url=document.getElementById('draw-admin-url'),publish=document.getElementById('draw-admin-publish'),status=document.getElementById('draw-admin-status'); let output='';
  fetch('weekly-draw-config.js?v='+Date.now(),{cache:'no-store'}).then(r=>r.text()).then(t=>{open.checked=/open:\s*true/.test(t);season.value=t.match(/season:\s*(\d+)/)?.[1]||new Date().getFullYear()+1;url.value=t.match(/entryUrl:\s*["']([^"']+)/)?.[1]||'join.html';}).catch(()=>{season.value=new Date().getFullYear()+1;url.value='join.html';});
  form.addEventListener('submit',e=>{e.preventDefault();const safeUrl=url.value.trim().replace(/\\/g,'\\\\').replace(/"/g,'\\\"');output=`/* Weekly Draw season switch.\n * Managed from admin.html. Previous-season winners remain on the page as an archive.\n */\nwindow.SUTTON_WEEKLY_DRAW = Object.freeze({\n  open: ${open.checked},\n  season: ${Number(season.value)},\n  entryUrl: "${safeUrl}"\n});\n`;publish.hidden=false;status.textContent=open.checked?`Prepared: ${season.value} entries OPEN.`:`Prepared: ${season.value} entries CLOSED.`;});
  document.getElementById('draw-admin-copy').addEventListener('click',async()=>{if(!output)return;await navigator.clipboard.writeText(output);status.textContent='Config copied — paste it into weekly-draw-config.js and commit.';});
})();
